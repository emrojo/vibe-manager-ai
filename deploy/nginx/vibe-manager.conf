# ====================================================================
# Vibe Manager AI - Production Nginx Configuration (Host Reverse Proxy)
# ====================================================================
# Place in: /etc/nginx/sites-available/vibe-manager.conf
# Symlink to: /etc/nginx/sites-enabled/vibe-manager.conf
#
# Replace 'your-domain.com' with your actual server domain or IP.
# ====================================================================

# Rate limiting zone for sensitive authentication endpoints
limit_req_zone $binary_remote_addr zone=vibe_auth_limit:10m rate=5r/s;

# Upstream definitions for internal microservices
upstream vibe_gateway_upstream {
    server 127.0.0.1:8080;
    keepalive 32;
}

upstream vibe_backend_upstream {
    server 127.0.0.1:8000;
    keepalive 32;
}

upstream vibe_frontend_upstream {
    server 127.0.0.1:3010;
    keepalive 32;
}

# --------------------------------------------------------------------
# 1. HTTP Server - Redirect all traffic to HTTPS
# --------------------------------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com;

    # Certbot ACME challenge location
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
    }

    # Redirect all HTTP requests to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# --------------------------------------------------------------------
# 2. HTTPS Server - Secure Reverse Proxy with WebSockets & Headers
# --------------------------------------------------------------------
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name your-domain.com;

    # SSL Certificates (managed by Certbot)
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/your-domain.com/chain.pem;

    # Modern SSL Security Settings (Mozilla Intermediate Profile)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_timeout 1d;
    ssl_session_cache shared:VibeSSL:10m;
    ssl_session_tickets off;
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 1.1.1.1 8.8.8.8 valid=300s;
    resolver_timeout 5s;

    # Security Headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # Max body size for prompt tasks and attachments (10MB)
    client_max_body_size 10M;
    client_body_buffer_size 128k;

    # Common Proxy Headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;

    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/xml+rss application/atom+xml image/svg+xml;

    # ----------------------------------------------------------------
    # A. Sensitive Auth Endpoints (Rate Limited)
    # ----------------------------------------------------------------
    location ~* ^/api/auth/(login|register) {
        limit_req zone=vibe_auth_limit burst=10 nodelay;
        proxy_pass http://vibe_backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }

    # ----------------------------------------------------------------
    # B. Real-Time WebSockets (Chat & Live Streaming Terminal Consoles)
    # ----------------------------------------------------------------
    # Covers /api/chat/ws and /api/processes/{id}/console
    location ~* ^/api/(chat/ws|processes/.+/console) {
        proxy_pass http://vibe_backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;

        # Keep persistent WebSocket connections alive for long sessions
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
        proxy_buffering off;
    }

    # ----------------------------------------------------------------
    # C. REST API & OpenAPI Documentation
    # ----------------------------------------------------------------
    location /api/ {
        proxy_pass http://vibe_backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_connect_timeout 60s;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    location /docs {
        proxy_pass http://vibe_backend_upstream;
        proxy_http_version 1.1;
    }

    location /openapi.json {
        proxy_pass http://vibe_backend_upstream;
        proxy_http_version 1.1;
    }

    # ----------------------------------------------------------------
    # D. Next.js Static Cache Optimization
    # ----------------------------------------------------------------
    location /_next/static/ {
        proxy_pass http://vibe_frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        expires 365d;
        access_log off;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # ----------------------------------------------------------------
    # E. Frontend Web Application (Fallback)
    # ----------------------------------------------------------------
    location / {
        proxy_pass http://vibe_frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_connect_timeout 30s;
        proxy_read_timeout 60s;
    }
}
